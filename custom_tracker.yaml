tracker_type: bytetrack

track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.3

# Longer memory so occluded people in a crowd don't get a new ID when they reappear
track_buffer: 90

match_thresh: 0.8
fuse_score: True